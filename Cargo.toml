[package]
name = "agrf"
version = "0.3.0"
edition = "2021"
description = "Stream of int/float to unicode braille graphs"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }

[profile.release]
lto = true
strip = true
